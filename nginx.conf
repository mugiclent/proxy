user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent"';

    access_log /var/log/nginx/access.log main;

    sendfile        on;
    keepalive_timeout  65;

    # ── TLS hardening (shared across all server blocks) ───────────────────────
    ssl_protocols             TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_session_cache         shared:SSL:10m;
    ssl_session_timeout       10m;

    # ── Proxy headers (shared across all server blocks) ───────────────────────
    proxy_http_version 1.1;
    proxy_set_header   Host              $host;
    proxy_set_header   X-Real-IP         $remote_addr;
    proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_set_header   Upgrade           $http_upgrade;
    proxy_set_header   Connection        "upgrade";

    # ── Security headers (shared across all server blocks) ───────────────────
    # HSTS — tells browsers to always use HTTPS for this domain for 1 year
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    # Prevents clickjacking — blocks the site from being embedded in iframes
    add_header X-Frame-Options "SAMEORIGIN" always;
    # Prevents MIME sniffing — browser must use the declared content-type
    add_header X-Content-Type-Options "nosniff" always;
    # Controls how much referrer info is sent when navigating away
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    # Restricts browser features — disable what the platform doesn't use
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # ── Docker internal DNS resolver ──────────────────────────────────────────
    # Required for plug-n-play upstreams — nginx resolves container names
    # per-request rather than at startup. Missing containers don't crash nginx;
    # they start working automatically once their container comes online.
    resolver 127.0.0.11 valid=30s ipv6=off;

    include /etc/nginx/conf.d/*.conf;
}
